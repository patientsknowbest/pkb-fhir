/**
 * Defines models for use in authorization policies.
 * 
 * These are useful for making authorization policies sane. Map FHIR resources into these types before feeding them 
 * into Oso (or whatever authorization library) 
 */
package ca.uhn.fhir.jpa.starter.auth.model; 